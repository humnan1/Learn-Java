Java Script can be used to make networking apps, games, mobile apps and command line tools.
Every browser has a Java Script engine, e.g. Chromes has v8, or can be run using Node.
ECMA Script= specification for Java

Right click, inspect, find console
Code editors to write Java Script code: Visual studio code, sublime text etc.
Node can be used to run java an dinstall third party libraries, so download?

console.log("hello");


Open visual studio code and start a new project, make a basic index.html page
node cna be used t uinstall thrid party libraries

New folder in visual studio code
Create a new file, to make a basic html page, rename the file index.html and type ! and click Tab
#Create a script line, should be at the end of body most of time
// this is a comment

<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>

<body>
    <h1> Hello World</h1>
    <script src = "index.js"></script>  // This shows that our java script code is now in index.js file
</body>

</html>

// create a new file inthe same folder called index.js, now put all the  java script code there.


//let keyord to determine a variable
//name is our variable

let name = "Chip";
console.log(name);
let age = 45;
let isApproved = true;
let firstName = undefined
let lastName - null;

//typeof operatore in console can tell ou what type of operatore something is e.g.
typeof name // should give "string"
// in javascript all the numbers, are of type numbers

let person = {
    name: "Pip",
    age = 13
}

console.log(person)

person["name"] = "Mary";

let selectedColours = ["red", "blue"]; // square brackets are an array
console.log(selectedColours[0]);  //red display on consle
selectedColours[2] = "green"; //added a third value to the array
console.log(selectedColours); //display on console

console.log(selectedColours.length); //nuber of items in array

function greeting(name, lastName) {
    console.log("hello " + name + " " + lastName);
}

greet("John", "Pip");
greet("Pip", "Chip");


functon square(number) {
    return number * number;
}

let number = square(2);
console.log(number);

/* this
is a
multiline comment */


//three ways to declare a variable in java script
var = myName = "Beau"

let ourName = "Pip"

const pi = 3.14 //const cant be chnaged


var a;
var b = 2;

a = 7;
b = a;
console.log (a)

var myVar = 87;
myVar++;  // my Var = myVar = 1
myVar--; // myVar = myVar - 1

var remainder; 
remainder = 11 % 3;


var a = 3;
var b = 17;
var c = 12;

a += 12; //a = a + 12
b += 9 // b = 9 + b 
c += 7 // c = c + 7

var a = 11;
var b = 9;
var c = 3;

a -= 6; // a = a - 6
b -= 15; // b = b - 15
c -= 1; // c = c - 1

var a = 5;
a *= 5; // a = a * 5

var a = 48;
a = a / 12;

var string = "This is \"double quotes \""  // or start the string eith double quotes

var newStr = "I am first";
newStr += "I come second."; // added an extra bt of string to our string 



function wordBlank(myNoun, myAdjective, myVerb, myAdverb) {
    var results = "";
    result += "The " + myAdjective + " " + myNoun + " " + myVerb + " to the store " + myAdverb."

    resturn result;
}

console.log(wordBlanks("dog", "big", "ran", "quickly"));


//nested array
var ourArray = [["the universe", 42], ["everything", 101010]];

var ourArray = ["Chip", "Pip", "Mip"];
ourArray.push(["Wip", "Tip"]);
// array had been extended

var ourArray = [1,2,3];
var removedFromOurArray = ourArray.pop()

ourArray.shift(); // removeds the first item
ourArray.unshift("agddgh");  // adds thta to th earray so  it id the first item



function ourFunctionWithArgs(a, b) {
    console.log(a - b);
}
ourFunctionWithArgs(10, 5); //recalling the function and replacing the placeholders with actual valus

function functionWithArgs(a, b) {
    console.log (a +b);
}
functionWithArgs(10, 5)


// Scope refers to the visibilty of variables, variales defined outside of a function block have gloabl scope so they can seen everwhere in the code



var outerwear = "T-Shirt";

function myOutfit() {

    return outerWear;
}
consol.log(myOutfit());


function minusseven(num) {
    return num -7;
}

console.log(minusSeven(10));

function timesFive(num)
    return num * 5;
}
 console.log(timesFive(5));


